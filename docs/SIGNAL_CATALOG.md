# TrendScan Signal Catalog

> **Comprehensive inventory of every signal / metric / indicator computed by the TrendScan codebase.**
>
> Generated by reading every file under `src/lib/{board,regime,factors,scanner}/`,
> `scripts/compute_crypto_factors.js`, and `scripts/build_snapshot.js`.
>
> Each entry documents: **name · file · what it measures · inputs · output ·
> used by · in signal engine? · predictive potential**.

## How to read this document

- **In signal engine?** — Whether the calculation is wired into the Factor
  Monitor's stance engine (`compositeEngine.computeFactorStance`) or the
  Macro Regime's allocation engine (`regimeSignals.computeAllocation`).
  These are the two "verdict engines" that produce actionable labels
  (CONSTRUCTIVE / SELECTIVE / DEFENSIVE / WAIT, or ALLOCATE / STABLECOINS).
- **Predictive potential** — A forward-looking judgment:
  - **HIGH** → directly drives a trend-continuation / trend-reversal call
    (e.g. adaptive z of price, factor spread z, regime quadrant flip).
  - **MEDIUM** → confirms or contextualises a primary signal (e.g. RSI,
    crowding score, breadth pct).
  - **LOW** → display-only metric (e.g. sparkline, YTD return, n-count).

---

## Status of the `src/lib/signal/compute.js` verdict engine

**Implemented (v3.1, backtested & tuned).** `src/lib/signal/compute.js` is the
production signal engine. The `/signal` route (`src/pages/Signal.jsx`) reads
pre-computed `signal_metrics` from `snapshot.json` (built server-side by
`scripts/compute_signal_metrics.js`) and renders STRONG / WEAK / NEUTRAL
verdicts for BTC, Majors (ETH/SOL/HYPE), and Cash.

The engine is a **weighted 9-signal scoring system** (not a pass/fail gate
chain — each signal contributes a directional weight plus optional
penalties/boosts to a composite stance + 0–10 confidence):

| # | Signal | Source function | Role |
|---|--------|-----------------|------|
| 1 | adaptiveZ (90/365 blend) | `adaptiveZ` | Price z-score, regime-aware (primary driver) |
| 2 | adaptiveZWithPctile | `adaptiveZWithPctile` | Non-parametric percentile (fat-tail detection) |
| 3 | trendTenure | `computeTrendTenure` | Consecutive days closing above 50-MA (persistence) |
| 4 | atrExt50ma | `computeAtrExt50ma` | Extension from 50-MA in ATR units (volatility-normalized) |
| 5 | RS vs BTC (7-day) | `computeRsVsBtc` | Return ratio for majors (confirmation) |
| 6 | fundingZ | `fundingZScore` | Funding rate z-score (crowding/reversal risk) — gate 6 is effectively dead in v3.1 (see code comment) |
| 7 | RSI (penalty only) | `computeRSI` | Overbought penalty (>80 reduces STRONG) |
| 8 | impulseZ (penalty only) | `computeImpulseZ` | Decelerating penalty (falling momentum) |
| 9 | macroZ (boost) | `computeMacroZ` | External signal, boosts confidence 7→8 when macroZ > 1.5 |

**Verdict mapping** (from `mapStanceToVerdict` + `DEFAULT_THRESHOLDS`):
- STRONG: stretched + persistent + confirmed → high confidence
- NEUTRAL: baseline
- WEAK: stretched negative + persistent bearish

**Backtest results** (2023-10 to 2025-07, 10-day forward, BTC/ETH/SOL — from file header):
- STRONG: 62.0% hit rate, +5.02% avg 10-day return (363 signals)
- WEAK:   54.1% hit rate, -1.37% avg 10-day return (61 signals)
- NEUTRAL baseline: +2.99% avg

The other verdict engines in the codebase are the **4-gate**
`compositeEngine.computeFactorStance` (stretch / persistence / crowding /
breadth → CONSTRUCTIVE / SELECTIVE / DEFENSIVE / WAIT) in
`src/lib/factors/compositeEngine.js`, and `regimeSignals.computeAllocation`,
which combines Ultra6 + OB1 + Core9 to emit ALLOCATE / STABLECOINS with
MAXIMUM / HIGH / STANDARD conviction.

The /signal page's pre-computed metrics come from `snapshot.json`'s
`signal_metrics` and `signal_history` keys (see README's snapshot keys
table). The pure compute functions are shared between the production
snapshot builder (`scripts/compute_signal_metrics.js`) and the historical
replay backtester (`scripts/signal/backtest.js`).

---

## Table of Contents

1. [Board engine — per-asset metrics & theme scoring](#1-board-engine)
2. [Regime signals — macro composite + Ultra6 + OB1 + allocation](#2-regime-signals)
3. [Regime calculations — statistical primitives](#3-regime-calculations)
4. [Regime percentile — non-parametric rank statistics](#4-regime-percentile)
5. [Regime rotation — quadrant flip detection](#5-regime-rotation)
6. [Regime engine — RISK-ON / RISK-OFF / MIXED](#6-regime-engine)
7. [Factor signals — FactorWatch cross-asset](#7-factor-signals)
8. [Seasonality — Ken French baselines](#8-seasonality)
9. [Change log — regime diff persistence](#9-change-log)
10. [Factors engine — factor stance verdicts](#10-factors-engine)
11. [Rotation detector (shared)](#11-rotation-detector-shared)
12. [Crowding matrix](#12-crowding-matrix)
12b. [Tradable-universe filter — pegged assets](#12b-tradable-universe-filter-pegged-assets)
13. [Narrative generator](#13-narrative-generator)
14. [Scanner factor engine — quintile portfolios + spread monitor](#14-scanner-factor-engine)
15. [Scanner calculations — EMA / VWAP / RSI](#15-scanner-calculations)
16. [Scanner scan engine — trend filter pipeline](#16-scanner-scan-engine)
17. [Server-side crypto factor pipeline](#17-server-side-crypto-factor-pipeline)
18. [Signal verdict engine — STRONG / WEAK / NEUTRAL (implemented)](#18-signal-verdict-engine)

---

## 1. Board engine

**File**: `src/lib/board/boardEngine.js` (1304 lines)
**Used by**: `src/pages/Board.jsx` → `DailyBoard`, `ThemesTab`, `MomentumTab`,
`BreadthTab`, `ExtensionTab`, `QuickViewBar`, `MomentumScanTab`,
`FactorQuilt`, `RiskPulse` (no separate component, returned in `riskPulse`).

### 1.1 `computeMetrics(candles)` — per-asset metric bundle

The single most-used per-asset calculation. Produces ~25 fields used by
almost every Board tab.

| Field | Measures | Inputs | Output | Used by | In signal engine? | Predictive |
|---|---|---|---|---|---|---|
| `price` | last close | candles | number | every tab | display | LOW |
| `ma20`, `ma50`, `ma200` | simple moving averages | closes | number or null | themes, regime, quickView | indirect (via `above20/50/200`) | MEDIUM |
| `above20/50/200` | trend filter booleans | price vs MA | 0/1/null | scoreTheme, regimeLabel, quickView | yes (via theme score) | HIGH |
| `ret1d/5d/20d/60d` | multi-horizon returns | closes | decimal | themes, momentum, quickView | partial (ret20d feeds theme score) | HIGH |
| `atr14` | 14-period Wilder ATR | highs/lows/closes | number | `atrExt50ma` | indirect | MEDIUM |
| `atrExt50ma` | price extension vs 50MA in ATR units | `(price - ma50) / atr14` | number | tooHot, cleanMomentum, themeStatus | yes (theme status) | HIGH |
| `adrPct` | average daily range % over 20d | highs/lows | % number | startingToMove, momentumScan | no | MEDIUM |
| `trendTenure` | consecutive days closing above 50MA | closes + rolling 50MA | integer | startingToMove, momentumScan | no | HIGH |
| `volRatio` | current vol / 20d avg vol | volumes | ratio | cleanMomentum, startingToMove, momentumScan | no | MEDIUM |
| `newHigh20d` | close = 20d high | highs | 0/1 | scoreTheme (leadership), quickView | yes (theme score) | HIGH |
| `newHigh52w` | close = 252d high | highs | 0/1 | scoreTheme, regime | yes (theme score) | HIGH |
| `distMa20/50/200` | % distance from MA | price vs MA | % number | tooHot, startingToMove, quickView | no | MEDIUM |
| `abs1w/1m/3m/6m` | absolute momentum (close / min-of-window) | closes | % | momentumScan rankings | no | MEDIUM |
| `rel1w/1m/3m/6m` | relative momentum (close / avg-of-window) | closes | % | momentumScan rankings | no | MEDIUM |
| `avgDollarVol20d` | 20d mean USD volume | close × vol | USD | momentumScan eligibility | no | LOW |
| `sparkline` | last 30 closes | closes | number[] | UI thumbnails | no | LOW |
| `rs_btc_20d` | 20d return minus BTC 20d return | ret20d vs BTC ret20d | decimal | themeScore (rsComponent), quickView, cleanMomentum | yes (theme score) | HIGH |

### 1.2 `scoreTheme(metrics, themeName)` — composite theme score (0–100)

**Measures**: weighted blend of breadth, leadership, momentum, relative strength.
**Inputs**: per-asset metrics from `computeMetrics` for all assets in a theme.
**Output**: `{score, pctAbove20, pctAbove50, pctAbove200, pctNewHigh,
pctNewHigh52w, avgRet20, avgRet5d, avgAtrExt, memberCount, avg_rs_btc_20d,
breadth, leadership, momentum}`.
**Weights**: `0.30*breadth + 0.25*leadership + 0.30*momentum + 0.15*rsComponent`
where:
- breadth = (pctAbove20 + pctAbove50) / 2
- leadership = pctNewHigh20 * 0.6 + pctAbove200 * 0.4
- momentum = scaleTo100(avgRet20, -0.10, 0.15)
- rsComponent = clamp(50 + avg_rs_btc_20d * 500, 0, 100)
**Used by**: `ThemesTab`, `DailyBoard`, `MomentumTab`, `buildThemeSectorRotation`.
**In signal engine?**: YES — feeds `themeStatus`.
**Predictive potential**: HIGH — broad theme leadership signal.

### 1.3 `themeStatus(score, delta, avgAtrExt)` — 8-state verdict label

**Measures**: categorical theme health (the only STRONG/WEAK/NEUTRAL-style
verdict in the current codebase).
**Output**: one of
`'STRONG / HOT' | 'DOMINANT' | 'EMERGING' | 'STRONG' | 'FADING' |
'WEAK' | 'IMPROVING' | 'DETERIORATING' | 'NEUTRAL'`.
**Logic**:
| Score | Delta | AvgAtrExt | Label |
|---|---|---|---|
| ≥75 | — | >6 | STRONG / HOT |
| ≥75 | — | — | DOMINANT |
| ≥60 | >2 | — | EMERGING |
| ≥60 | — | — | STRONG |
| ≤35 | <-2 | — | FADING |
| ≤35 | — | — | WEAK |
| — | >3 | — | IMPROVING |
| — | <-3 | — | DETERIORATING |
| — | — | — | NEUTRAL |

**Used by**: `ThemesTab` (status pill), `DailyBoard` (ThemeStatus summary).
**In signal engine?**: YES — this IS the verdict.
**Predictive potential**: HIGH — already produces STRONG/WEAK/NEUTRAL-style calls.

### 1.4 `computeThemeScoreAtOffset(rawResults, offset)` — historical theme score

**Measures**: theme score N days ago (for rotation detection).
**Inputs**: rawResults + candle arrays + offset days.
**Output**: `{themeName: score, ...}`.
**Used by**: `buildThemeRotation` (5-day lookback).
**Predictive potential**: MEDIUM — drives rotation detection.

### 1.5 `buildThemeRotation(rawResults, themes)` — top 5 climbers + fallers

**Measures**: 5-day change in theme score.
**Output**: `{climbers: [...5], fallers: [...5], lookbackDays: 5}`.
**Used by**: `ThemesTab` (rotation panel).
**Predictive potential**: HIGH — leading rotation indicator.

### 1.6 `buildStartingToMove(rawResults)` — inflection-point asset list

**Measures**: assets with rising RS vs BTC but not yet stretched.
**Filter**: `distMa50 ∈ (0, 15)`, sorted by RS delta, top 20.
**Output**: array of `{symbol, name, theme, rsNow, rsDelta, distMa50,
ret20d, volRatio, adrPct, trendTenure, price}`.
**Used by**: `MomentumTab` (Starting to Move table).
**Predictive potential**: HIGH — early-stage breakout detector.

### 1.7 `buildMomentumScan(rawResults)` — top-25 by horizon

**Measures**: absolute + relative momentum rankings at 1W/1M/3M/6M.
**Output**: `{1W, 1M, 3M, 6M: Array<rank, symbol, ...metrics>}`.
**Used by**: `MomentumScanTab`.
**Predictive potential**: MEDIUM — confirmation/ranking only.

### 1.8 `buildStyleRotation(rawResults)` — paired-asset rotation

**Measures**: ret1d/5d/20d spread between ROTATION_PAIRS (e.g. ETH/BTC,
SOL/BTC, DOGE/BTC).
**Output**: array of `{label, desc, ret1d, ret5d, ret20d}`.
**Used by**: `MomentumTab` (Style Rotation panel).
**Predictive potential**: MEDIUM — risk-on/off proxy.

### 1.9 `buildRiskPulse(rawResults)` — 5-asset risk dashboard

**Measures**: ret1d/5d/20d + distMa50 for ETH/BTC, DOGE/BTC, BTC, SOL, LINK.
**Output**: array of `{label, context, ret1d, ret5d, ret20d, distMa50}`.
**Predictive potential**: MEDIUM — regime confirmation.

### 1.10 `buildThemeSectorRotation(themes)` — sorted theme RS table

**Measures**: themes sorted by `avg_rs_btc_20d` (descending).
**Output**: array of `{theme, rs_btc_20d, ret5d, ret20d, score, status}`.
**Used by**: `DailyBoard` (Theme Sector Rotation panel).
**Predictive potential**: MEDIUM.

### 1.11 `buildBreadthSeries(rawResults)` — daily advancers/decliners + new-high series

**Measures**: 30-day breadth series.
**Output**: `{themeBreadth: [...], dailySeries: [{day, advancers,
decliners, adDiff, total}], dates, newHigh20dSeries: [...]}`.
**Used by**: `BreadthTab`.
**Predictive potential**: HIGH — breadth thrusts are classic regime-shift signals.

### 1.12 `buildRegimeLabel(regime)` — 4-state market regime

**Measures**: crypto-native risk regime from % assets above MA.
**Output**: `{label: 'RISK-ON'|'RISK-ON/NARROW'|'RISK-OFF'|'MIXED', color}`.
**Logic**:
- pctAbove50 ≥ 60 AND pctAbove200 ≥ 55 → RISK-ON (green)
- pctAbove50 ≥ 50 → RISK-ON/NARROW (blue)
- pctAbove50 ≤ 35 → RISK-OFF (red)
- else → MIXED (neutral)

**Used by**: `DailyBoard` (header).
**In signal engine?**: YES — direct verdict.
**Predictive potential**: HIGH.

### 1.13 `buildQuickView(rawResults, hlTickers)` — 5-bucket market summary

Five named buckets, each top-5 by an internal score:

| Bucket | Measures | Filter | Sort key |
|---|---|---|---|
| `strongest` | composite of ret20d (40%) + rs_btc_20d*100 (30%) + trendScore*5 (30%) | none | composite desc |
| `pickingUp` | recent acceleration | ret1w > 0 AND ret1w > ret20d/4 | (ret1w − ret20d/4) desc |
| `crowded` | long crowding via Hyperliquid funding | funding > 0.00005 AND crowdingScore > 10 | crowdingScore desc |
| `washedOut` | oversold candidates | ret60d < −0.15 | ret60d asc |
| `bigMoves` | largest |ret1d| | abs(ret1d) > 0.03 | abs(ret1d) desc |

**Used by**: `QuickViewBar` (top of Board).
**In signal engine?**: YES — `crowded` bucket annualises funding (`*3*365*100`)
  which is the only place funding rate currently becomes a tradeable signal
  in the codebase.
**Predictive potential**: HIGH — `crowded` is the closest existing
  funding-based signal; `washedOut` is mean-reversion; `strongest` is trend.

### 1.14 `computeTradMetrics(candles)` — TradFi OHLCV bundle

Same as `computeMetrics` minus crypto-specific momentum/ad fields. Used by
TradFi tab. Not currently fed into any verdict engine.

---

## 2. Regime signals

**File**: `src/lib/regime/regimeSignals.js` (407 lines)
**Used by**: `src/pages/MacroRegime.jsx` → `RegimeCard`, `CompositeGauge`,
`AllocationPanel`, `MacroNarrativeBanner`, `IndicatorList`, `MacroCharts`.

### 2.1 `computeGrowthSignals(data)` — 9 inputs → growth composite

| # | Name | Source | What it measures | Weight |
|---|---|---|---|---|
| G1 | BTC 90D ROC | btcPrice | 90-day % change of BTC price | 1.75 |
| G2 | ETH Momentum | ethPrice | 60/180 adaptive z of ETH | 1.50 |
| G3 | ETH/BTC Ratio | ethBtcRatio | 60/180 adaptive z | 1.25 |
| G4 | Fear & Greed | fearGreed | 30/90 adaptive z | 1.00 |
| G5 | Volume Trend | btcVolume | 30/90 adaptive z of volume | 0.75 |
| G6 | USDT.D (inv) | usdtDominance | 30/90 adaptive z, inverted | 1.25 |
| G7 | BTC > 200MA | btcPrice + sma(200) | long-term trend boolean | 1.00 |
| G8 | HY Spread (inv) | FRED BAMLH0A0HYM2 | 52/104 adaptive z, inverted | 1.50 |
| G9 | Claims (inv) | FRED ICSA | 13/52 adaptive z, inverted | 1.50 |

**Output**: array of `{name, value (z-score), weight, raw, requiresFred?}`.
**Composite**: weighted by `weightedComposite()` from regimeCalculations.
**Classification**: `classifyGrowthRegime(z)` →
`'BOOM' (z≥1) | 'EXPANSION' (z≥0.5) | 'RECESSIONARY' (z≤-0.5) | 'NEUTRAL'`.
**In signal engine?**: YES — feeds Ultra6 + Core9.
**Predictive potential**: HIGH — composite of macro + on-chain.

### 2.2 `computeInflationSignals(data)` — 8 inputs → inflation composite

| # | Name | Source | What it measures | Weight |
|---|---|---|---|---|
| I1 | BTC/Gold ROC | btcPrice + goldPrice | 90d ROC of BTC/gold ratio | 1.50 |
| I2 | Gold YoY | goldPrice | 365d ROC + 90/365 adaptive z | 1.75 |
| I3 | BTC Volatility | btcPrice | 30/90 adaptive z | 0.75 |
| I4 | USDT.D | usdtDominance | 30/90 adaptive z | 1.00 |
| I5 | F&G (inv) | fearGreed | inverse 30/90 z | 0.75 |
| I6 | 10Y Breakeven | FRED T10YIE | 52/104 adaptive z | 1.75 |
| I7 | 5Y5Y Forward | FRED T5YIFR | 52/104 adaptive z | 1.50 |
| I8 | CPI Change | FRED CPIAUCSL | 12m point change × direction | 1.00 |

**Classification**: `classifyInflationRegime(z)` →
`'HOT' | 'REINFLATION' | 'DISINFLATION' | 'NEUTRAL'`.
**Predictive potential**: HIGH — drives quadrant classification.

### 2.3 `computeLiquiditySignals(data)` — 9 inputs → liquidity composite

| # | Name | Source | What it measures | Weight |
|---|---|---|---|---|
| L1 | BTC.D ROC (inv) | btcDominance | 30/90 z, inverted | 1.25 |
| L2 | USDT.D (inv) | usdtDominance | 30/90 z, inverted | 1.50 |
| L3 | Market Cap Trend | btcPrice (proxy) | 30/90 adaptive z | 1.50 |
| L4 | Fear & Greed | fearGreed | 30/90 adaptive z | 1.00 |
| L5 | ETH/BTC Ratio | ethBtcRatio | 30/90 adaptive z | 1.25 |
| L6 | Vol/Cap Ratio | btcVolume + btcPrice | boolean: volROC > 0 | 0.75 |
| L7 | Fed Net Liq | FRED FED_NET_LIQ | 52/104 adaptive z | 2.00 |
| L8 | M2 ROC | FRED M2SL | 13/52 adaptive z | 1.75 |
| L9 | NFCI (inv) | FRED NFCI | 13/52 adaptive z, inverted | 1.50 |

**Classification**: `classifyLiquidityRegime(z)` →
`'VERY LOOSE' | 'LOOSE' | 'TIGHT' | 'NEUTRAL'`.
**Predictive potential**: HIGH — liquidity is the single biggest crypto driver.

### 2.4 `computeUltra6(data, growthNowcast, liqMeZ, quadrant, liquidity)` — 6-gate allocation signal

**Measures**: high-conviction "all systems go" composite.
**Output**: `{signals: {U1..U6}, score (0-6), on: score >= 4}`.

| Gate | Signal | Logic |
|---|---|---|
| U1 | macroFavorable | quadrant ∈ {GOLDILOCKS, OVERHEAT} |
| U2 | liqLoose | liqMeZ > 0 |
| U3 | btcAbove50MA | btcClose > sma(btcPrice, 50) |
| U4 | ethBtcPositive | pctROC(ethBtcRatio, 8) > -1 |
| U5 | growthPositive | growthNowcast > 50 |
| U6 | btcDomDecline | pctROC(btcDominance, 8) < 0 |

**Used by**: `AllocationPanel` (Allocation logic).
**In signal engine?**: YES — gate 1 of 2 for ALLOCATE.
**Predictive potential**: HIGH — primary risk-on gate.

### 2.5 `computeOB1Signals(data)` — 6-gate on-balance-volume confirmation

**Measures**: volume/flow confirmation of Ultra6.
**Output**: `{signals: {OB1_*}, score (0-6), on: score >= 3}`.

| Gate | Signal | Logic |
|---|---|---|
| OB1_obvRising | OBV slope over 13d > 0 | computeOBV(ethPrice, btcVolume) |
| OB1_volAccel | 4d vol ROC > 13d vol ROC | acceleration check |
| OB1_btcVolAbove20ma | btcVolCurrent > sma(btcVolume, 20) | volume expansion |
| OB1_usdtDomFalling | pctROC(usdtDominance, 8) < 0 | risk-on flow |
| OB1_rsiRecovering | ETH RSI(14) > prior RSI(14) | momentum turn |
| OB1_ethBtcStable | pctROC(ethBtcRatio, 8) >= -1 | alt tolerance |

**Used by**: `AllocationPanel`.
**In signal engine?**: YES — gate 2 of 2 for ALLOCATE.
**Predictive potential**: HIGH — volume confirmation.

### 2.6 `computeAllocation(ultra6, ob1, core9Score, btcPrice)` — final allocation verdict

**Measures**: top-level allocation call.
**Output**:

| Condition | Status | Vehicle | Conviction |
|---|---|---|---|
| NOT (ultra6.on AND ob1.on) | STABLECOINS | null | NONE |
| core9Score ≥ 8 AND btcAbove50MA | ALLOCATE | T3 BASKET | MAXIMUM |
| core9Score ≥ 7 | ALLOCATE | BTC | HIGH |
| else (both on) | ALLOCATE | BTC | STANDARD |

**Used by**: `AllocationPanel`.
**In signal engine?**: YES — this IS the verdict (crypto-native analogue of
STRONG/WEAK/NEUTRAL).
**Predictive potential**: HIGH.

### 2.7 `computeCore8Score(ultra6)` — alias for `ultra6.score`

**Measures**: 6-gate count (despite the name "Core8").
**Output**: integer 0–6.

### 2.8 `computeCore9Score(data, growthSignals)` — quality overlay

**Measures**: `growthSignals.filter(s.value > 0).length + 3`, capped at 9.
**Used by**: `computeAllocation` for conviction tier.
**Predictive potential**: MEDIUM.

---

## 3. Regime calculations

**File**: `src/lib/regime/regimeCalculations.js` (291 lines)
**Used by**: every regime + factors + scanner/factorEngine file. These are
the **statistical primitives** the whole site is built on.

### 3.1 `mean(arr)`, `stddev(arr, mu?)`, `sma(series, period)`, `rollingStd(series, period)`

Standard statistical helpers. Output: number.

### 3.2 `adaptiveZ(series, shortLen=90, longLen=365, shortWeight=0.60)` — **the core z-score**

**Measures**: where the current value sits relative to its own recent
vs long-term history. Blends short-term responsiveness with long-term structure.
**Inputs**: numeric series, short/long lookback windows, short weight.
**Output**: z-score (typically −3 to +3, clamped to ±3 in some callers).
**Formula**: `shortWeight * shortZ + (1-shortWeight) * longZ`.
**Used by**: every growth/inflation/liquidity signal in §2.
**In signal engine?**: YES — every composite signal starts here.
**Predictive potential**: HIGH — the canonical "is this unusual?" test.

### 3.3 `pctROC(series, n=13)`, `yoyROC(series, n=365)`, `pointChange(series, n=13)`

Rate-of-change helpers. Output: number (pctROC returns %).

### 3.4 `ema(series, period)`, `currentEMA(series, period)`

Standard EMA. Output: array (ema) or number (currentEMA).

### 3.5 `zToScore(z)` — z-score → 0-100 scale

**Formula**: `clamp(50 + z*10, 0, 100)`.
**Used by**: `computeNowcast`, gauge UIs.

### 3.6 `impulseZ(compositeSeries, deltaPeriod=13, normPeriod=52)` — speed of change

**Measures**: how fast the composite z is changing relative to its own
historical deltas.
**Output**: z-score clamped to ±3.
**Used by**: `computeNowcast` (50% weight).
**Predictive potential**: HIGH — leading indicator of regime shifts.

### 3.7 `weightedComposite(signals)` — weighted-blend z-score

**Measures**: weighted average of multiple z-scored signals, each clamped to ±3.
**Inputs**: `Array<{value, weight}>`.
**Output**: composite z-score.
**Used by**: every `computeXxxSignals()` to roll up individual signals.
**Predictive potential**: HIGH.

### 3.8 `computeNowcast(compositeHistory)` — regime nowcast score

**Measures**: 50/50 blend of current z (`meZ`) and `impulseZ`.
**Output**: `{meZ, impulseZ, nowcast (0-100), score (0-100)}`.
**Used by**: `MacroRegime` for growth/inflation/liquidity gauges.
**In signal engine?**: YES — feeds quadrant + liquidity classification.
**Predictive potential**: HIGH.

### 3.9 `classifyQuadrant(growNowcast, inflNowcast)` — 5-state macro quadrant

**Output**:
- gUp + iDown → GOLDILOCKS
- gUp + iUp → OVERHEAT
- gDown + iUp → STAGFLATION
- gDown + iDown → CONTRACTION
- else → TRANSITIONAL

**Threshold**: MIDLINE=50, BAND=5.
**In signal engine?**: YES — drives Ultra6 U1.
**Predictive potential**: HIGH.

### 3.10 `classifyLiquidity(liqNowcast)` — 3-state liquidity

**Output**: `'LOOSE' | 'NEUTRAL' | 'TIGHT'` (band ±5 around 50).

### 3.11 `classifyRegime(growNowcast, inflNowcast, liqNowcast)` — combined label

**Output**: `{quadrant, liquidity, label: 'QUADRANT + LIQUIDITY', key}`.

### 3.12 `computeGrandComposite(growNowcast, inflNowcast, liqNowcast)`

**Weights**: 0.33 / 0.33 / 0.34.
**Output**: number 0–100.
**Used by**: `RegimeCard` headline.

### 3.13 `computeRSI(series, period=14)` — Wilder's RSI

**Output**: 0–100 (or 50 if insufficient data).
**Used by**: `computeOB1Signals` (ETH RSI), `Scanner` RSI filter.
**In signal engine?**: YES (in OB1) — partial.
**Predictive potential**: HIGH — classic overbought/oversold.

### 3.14 `computeOBV(prices, volumes)` — On-Balance Volume

**Output**: cumulative volume series.
**Used by**: `computeOB1Signals`.
**Predictive potential**: HIGH — divergence vs price is a classic reversal signal.

### 3.15 `getNextExecutionDate()` — first Friday of next month

Utility for the Allocation panel. Not a signal.

### 3.16 `SEASON_LABELS`, `REGIME_COLORS`, `REGIME_BG_OPACITY`

Display constants. Map quadrant → season (SPRING/SUMMER/FALL/WINTER/FLUX).

---

## 4. Regime percentile

**File**: `src/lib/regime/regimePercentile.js` (219 lines)
**Used by**: `factorEngine.computeSpreadMonitor`, all factor spread z+%
calculations. Borrowed from factorwatch.ai methodology.

### 4.1 `adaptiveZWithPctile(series, shortLen=90, longLen=365, lookback=252)`

**Measures**: adaptive z-score + non-parametric percentile against trailing
252 overlapping windows.
**Output**: `{z, pctile (0-100)}`.
**Why**: z-scores assume normality; financial returns are fat-tailed. Pctile
keeps the z honest.
**In signal engine?**: YES — feeds factor spread monitor.
**Predictive potential**: HIGH — non-parametric extremes are rarer than
z=2 in calm regimes.

### 4.2 `percentileOf(current, history)` — simple rank

**Output**: 0–100 (50 = median).

### 4.3 `horizonReturn(prices, horizonDays)` — h-day compounded return

**Output**: decimal return (0.05 = +5%) or null.

### 4.4 `horizonReturnWithStats(prices, horizonDays, lookback=252)`

**Measures**: h-day return + z-score + percentile vs trailing overlapping windows.
**Output**: `{ret, z, pctile}` or null.
**Used by**: `factorEngine.computeSpreadMonitor` for 1d/5d/20d/60d horizons.
**In signal engine?**: YES — primary input to `computeFactorStance`.
**Predictive potential**: HIGH — multi-horizon extreme detection.

---

## 5. Regime rotation

**File**: `src/lib/regime/regimeRotation.js` (153 lines)
**Used by**: `MacroRegime` (regime card flip banner).

### 5.1 `detectRegimeRotation(history)` — quadrant flip detector

**Measures**: 3-session-confirm + 10-session-fresh flip detection on
macro regime quadrant history.
**Inputs**: `[{date, quadrant, growth, inflation, liquidity}]`.
**Output**: `{currentLabel, previousLabel, heldSessions, flipped,
flipFlag, flipConfirmedAt, previousHeldSessions, confirmSessions: 3,
freshSessions: 10}`.
**Rules**:
- Flip flagged only if new quadrant holds ≥3 sessions AND displaced
  quadrant had itself held ≥3 sessions.
- Flag stays "fresh" (visible) for 10 sessions after confirmation.
**Predictive potential**: HIGH — confirmed regime shifts are the highest-quality macro signal.

### 5.2 `detectAxisRotation(history)` — per-axis flip detector

Runs `detectRegimeRotation` separately on growth / inflation / liquidity
labels. Returns `{growth, inflation, liquidity}` rotation states.

---

## 6. Regime engine

**File**: `src/lib/regime/regimeEngine.js` (121 lines)
**Used by**: `regimeData.getRegimeData`, indirectly by `DailyBoard`.

### 6.1 `computeRegime(signals)` — RISK-ON / RISK-OFF / MIXED

**Inputs**: `{btcTrend, ethTrend, altBreadth, btcDominance,
marketBreadth, dollarFlow, volatility}` (each −1 to 1 or 0 to 1).
**Weights**: btcTrend 0.20, ethTrend 0.15, altBreadth 0.20,
btcDominance (inv) 0.15, marketBreadth 0.15, dollarFlow 0.10,
volatility (inv) 0.05.
**Output**: `{regime: 'RISK_ON'|'RISK_OFF'|'MIXED', score (−1..1), label}`.
**Threshold**: |score| > 0.15 → ON/OFF, else MIXED.
**In signal engine?**: YES — direct verdict.
**Predictive potential**: HIGH.

### 6.2 `buildRegimeSignals(data)` — derive inputs from board data

Maps board `benchmarks` + `regime` into the 7 signals above. BTC/ETH trend =
`(distMa20 > 0 && distMa50 > 0) → 1 / both < 0 → -1 / else 0`.
**Output**: `{regime, score, label, signals}`.
**Predictive potential**: MEDIUM (just glue).

### 6.3 `scoreToGauge(score)` — score −1..1 → 0..100

`((score + 1) / 2) * 100`. Display helper.

---

## 7. Factor signals (FactorWatch cross-asset)

**File**: `src/lib/regime/factorSignals.js` (152 lines)
**Used by**: `useFactorSignals` hook → `MacroNarrativeBanner`,
`CrossAssetDivergenceChart`, `RevisionArbitrageTable`,
`TradFiThematicProxy`.
**Gate**: Requires `VITE_ENABLE_FACTORWATCH=true` to render, but the
engine itself runs whenever `snapshot.factor_watch` is present.

### 7.1 `computeFactorSignals(fwData, leaderHistory)` — unified TradFi stance

**Inputs**: FactorWatch sp500 + fw3000 factor z-scores + revision spreads
+ leader history.
**Output**:

| Field | Description |
|---|---|
| `isShakeout` | boolean: sp500 mom 5d σ ≤ −2.0 AND revision spread ≥ 20 |
| `isJunkRally` | boolean: size revision spread ≤ −20 |
| `liquidityFunnel` | 'NEUTRAL' / 'MEGA_CAP_SHIELDING' / 'BROAD_RISK_ON' (from sp500 vs fw3000 σ diff, ±0.3 deadzone) |
| `momentumStance` | `computeFactorStance` output for S&P 500 momentum |
| `sizeStance` | `computeFactorStance` output for S&P 500 size |
| `fwRotation` | `detectRotation(leaderHistory)` — real 3-session rotation |
| `primaryStance` | higher-confidence of momentum/size stances |
| `raw` | sp500 mom 5d σ, mom rev spread, size rev spread, fw3000 mom 5d σ, σ diff |

**Thresholds** (exported as `SIGNAL_THRESHOLDS`):
- `SHAKEOUT_MOM_SIGMA`: −2.0
- `SHAKEOUT_REV_SPREAD`: 20
- `JUNK_RALLY_SIZE_SPREAD`: −20
- `FUNNEL_DEADZONE`: 0.3

**In signal engine?**: YES — uses `computeFactorStance` directly.
**Predictive potential**: HIGH — the only TradFi cross-asset signal on the site.

### 7.2 `BASKET_TO_CRYPTO_THEME` mapping

Maps 15 FactorWatch equity baskets to crypto themes (e.g. 'Magnificent Seven' → 'Layer 1', 'Capital Markets Cycle' → 'DeFi').

---

## 8. Seasonality

**File**: `src/lib/regime/seasonality.js` (124 lines)
**Used by**: `MacroRegime` (seasonality panel).

### 8.1 `computeSeasonality(kenFrench)` — calendar-month baselines

**Inputs**: Ken French monthly factor returns (from `snapshot.ken_french`).
**Output**: `{market: [...12], smb: [...12], hml: [...12]}` where each
entry = `{month, full: {mean_pct, median_pct, hit_rate, n},
trailing_30y: {...}}`.
**Predictive potential**: MEDIUM — long-run baselines (n≥30) are noisy in
crypto but useful for context.

### 8.2 `formatSeasonalityBaseline(baseline, windowKey)` — display string

`"June: +1.0% mean / 70% hit rate (n=30, 30y)"`.

### 8.3 `getCurrentMonthBaseline(seasonality, factor)` — current month row

---

## 9. Change log

**File**: `src/lib/regime/changeLog.js` (259 lines)
**Used by**: `MacroRegime` → `ChangeBanner`.

### 9.1 `saveSnapshot(regime)`, `loadLastSnapshot()`, `getLastSnapshotTime()`, `timeAgo(ts)`, `clearSnapshot()`

localStorage persistence helpers (key: `trendscan_last_regime`).

### 9.2 `computeChanges(current, previous)` — diff two regime states

**Output**: array of human-readable change strings. Detects:
1. Quadrant flips: `"Regime shifted: GOLDILOCKS → OVERHEAT"`
2. Nowcast extreme breaches (≥70 or ≤30): `"growth entered bullish extreme (nowcast 72.3)"`
3. Per-axis label changes: `"liquidity label: NEUTRAL → LOOSE"`
4. Grand composite 50-cross: `"Grand composite crossed above 50"`
5. FRED availability toggle.
**Predictive potential**: LOW (display only, but very useful UX).

### 9.3 `diffAndPersist(current)` — convenience wrapper

Computes changes vs stored previous, then saves current.

---

## 10. Factors engine

**File**: `src/lib/factors/compositeEngine.js` (256 lines)
**Used by**: `factorSignals.computeFactorSignals`, `FactorSignalCard`,
`FactorMonitor`. This is the **only existing 4-gate verdict engine** (the
closest analogue to the not-yet-existing /signal "9-gate" engine).

### 10.1 `computeFactorStance({spreadZ, spreadPctile, rotation, crowdingScore, confirmation, factorName})`

**Measures**: turns factor z-score + rotation + crowding + breadth into a
CONSTRUCTIVE / SELECTIVE / DEFENSIVE / WAIT verdict with 0–10 confidence.

**Gates** (4 total, not 9):

| # | Gate | Threshold | Logic |
|---|---|---|---|
| 1 | stretch | \|z\| ≥ 2.0 | significant move vs history |
| 2 | persistence | `heldSessions >= confirmSessions` (3) | established leadership (2026-08-29 audit: previously read `rotation.confirmed`, which was structurally always false — the gate could never pass and the signal was permanently WAIT) |
| 3 | crowding | `crowdingScore < 0.7` | not too correlated with other factors |
| 4 | breadth | `confirmation >= 0.5` | >50% of quintile members agreeing |

**Verdict matrix**:

| Stretch | Persistence | Crowding | Breadth | Stance | Confidence |
|---|---|---|---|---|---|
| ✓ | ✓ | ✓ | ✓ | CONSTRUCTIVE | 9 |
| ✓ | ✓ | ✓ | ✗ | CONSTRUCTIVE | 7 |
| ✓ | ✓ | ✗ | — | SELECTIVE | 5 |
| ✓ | ✗ | — | — | WAIT | 3 |
| ✗ | ✓ | — | — | SELECTIVE | 5 |
| ✓ (negative z) | — | — | — | DEFENSIVE | 6 |
| ✗ | ✗ | — | — | WAIT | 2 |

**Cap**: crowded + CONSTRUCTIVE → SELECTIVE, confidence ≤5.

**Output**: `{stance, confidence (0-10), rationale: string[], color, gates,
raw: {spreadZ, spreadPctile, crowdingScore, confirmation, passCount}}`.

**Used by**: `factorSignals`, `FactorMonitor`, `FactorSignalCard`.
**In signal engine?**: YES — this IS the verdict engine.
**Predictive potential**: HIGH — the primary "should I act on this factor"
signal on the site.

### 10.2 `computeAllStances(spreadMonitorRows, rotation, crowdingMatrix)`

Runs `computeFactorStance` for every factor in the spread monitor, returns
the highest-confidence one as `primary` plus all stances sorted by confidence.

### 10.3 Constants

- `STRETCH_THRESHOLD = 2.0`
- `CROWDING_THRESHOLD = 0.7`
- `CONFIRMATION_THRESHOLD = 0.5`
- `STANCE`, `STANCE_COLORS` enums.

---

## 11. Rotation detector (shared)

**File**: `src/lib/factors/rotationDetector.js` (191 lines)
**Used by**: `factorSignals`, `compute_crypto_factors.js`.

### 11.1 `detectRotation(history)` — generalized leader-flip detector

Run-based semantics (rewritten 2026-08-29): walks back from the latest
entry to find the current label's run. `flipped` = took over from a
different label; `confirmed` = flip + `CONFIRM_SESSIONS` held + the
displaced leader was established (≥3 sessions) — a flip-specific
judgement. NOTE: this deliberately diverges from
`regimeRotation.detectRegimeRotation` (macro quadrants), which retains
the older compare-today-vs-yesterday pattern.

Asset-class-agnostic — accepts either `{leader}` or `{quadrant}` keys.

**Constants**: `CONFIRM_SESSIONS = 3`, `FLIP_FRESH_SESSIONS = 10`.

**Output**: `{currentLabel, previousLabel, heldSessions, flipped,
flipFlag, flipConfirmedAt, previousHeldSessions, confirmed,
confirmSessions, freshSessions}`.

**Predictive potential**: HIGH — both crypto factors and macro regimes use this.

### 11.2 `buildHistoryEntry(date, leader)`, `loadFactorHistory(key)`, `saveFactorHistory(key, history)`, `appendToHistory(history, date, leader)`

localStorage persistence (capped at 90 entries).

---

## 12. Crowding matrix

**File**: `src/lib/factors/crowdingMatrix.js` (222 lines)
**Used by**: `compute_crypto_factors.js`, `FactorMonitor`.

### 12.1 `buildCrowdingMatrix(spreadSeriesByFactor, window=90)`

**Measures**: pairwise Pearson correlation of factor spread return series.
**Output**: `{matrix, maxCorrelation(factor), avgCorrelation(factor)}`.
**Used by**: `compositeEngine.computeFactorStance` (crowdingScore input).
**In signal engine?**: YES — Gate 3 of `computeFactorStance`.
**Predictive potential**: HIGH — crowding is the "are these five bets
actually one bet?" check.

### 12.2 `extractSpreadSeries(portfoliosByFactor, candlesBySymbol, window=90)`

Builds daily Q5−Q1 spread return series per factor from candle data.
Equal-weighted Q5/Q1 portfolios, normalised to start at 1.0.
**Output**: `{factor: number[]}`.

---

## 12b. Tradable-universe filter (pegged assets)

**File**: `src/lib/factors/universeFilter.js` (127 lines)
**Used by**: `compute_crypto_factors.js` (both CMC and CoinGecko paths),
`FactorMonitor` (live-refresh path).

### 12b.1 `filterTradableUniverse(coins)` / `isExcludedAsset(asset)`

**Measures**: whether an asset is tradable for factor computation. Excludes
stablecoins (symbol denylist + name patterns + CMC `stablecoin` tags),
tokenized gold/silver, wrapped/staked derivatives (wBTC, stETH, ...), and
exchange revenue tokens (LEO, OKB, BGB, KCS, GT).

**Why** (2026-08-29 audit): 12 pegged/native assets previously sat in the
factor universe — 9 stablecoins occupied "Low Volatility" Q1 and their
near-zero vol / huge turnover corrupted the volatility + liquidity factors.

**Input**: `Array<{symbol, name?, tags?}>` (CMC-shaped or bare).
**Output**: filtered array (same shape).
**In signal engine?**: YES — gates the entire factor universe upstream of
every factor score.
**Predictive potential**: n/a (data hygiene, not a signal).

---

## 13. Narrative generator

**File**: `src/lib/factors/narrativeGenerator.js` (135 lines)
**Used by**: `FactorSignalCard`, `FactorMonitor`.

### 13.1 `generateFactorNarrative(stance, factorName)`

Returns `{headline: 'MOMENTUM — CONSTRUCTIVE (7/10)', detail: rationale joined by ·, color}`.

### 13.2 `generateRotationNarrative(rotation)`

Plain-English rotation status: "Rotation confirmed: momentum → size, 6 sessions ago" / "Unconfirmed flip: ..., 1/3 sessions (2 more to confirm)" / "momentum leading for 6 sessions".

### 13.3 `generateSignalCard({factorName, stance, rotation})`

Combines factor + rotation narratives for a signal card.

### 13.4 `generateChangeNarrative(prev, curr)`

Diff two factor states. Detects:
- Leader change: `"Factor leadership: momentum → size"`
- Stance change: `"momentum stance: WAIT → CONSTRUCTIVE"`
- New stretch: `"size new stretch signal (z=2.4)"`

---

## 14. Scanner factor engine

**File**: `src/lib/scanner/factorEngine.js` (592 lines)
**Used by**: `compute_crypto_factors.js` (server-side), `FactorMonitor`
(client-side).

### 14.1 `computeFactorScores(universe)` — 5-factor scorecard per asset

| Factor | Formula | Analog |
|---|---|---|
| `momentum` (12-1mo) | `P(t-21d) / P(t-252d) - 1` (clamped [−0.95, 5.0]) | Momentum |
| `size` | `-log(marketCap)` (small = high score) | Size |
| `volatility` | `-stddev(daily returns, 30d)` (low vol = high score) | Low Vol |
| `beta` | `1 - OLS β vs BTC over 90d` (low beta = high score) | Low Beta |
| `liquidity` | `log(30d USD volume / marketCap)` (clamped [−10, 5]) | Liquidity |

**Inputs**: `Array<{symbol, candles, marketCap}>`.
**Output**: `Array<{symbol, scores: {momentum, size, volatility, beta, liquidity}}>`.

### 14.2 `buildQuintilePortfolios(scoredUniverse, factorName)`

Winsorize (2.5/97.5 pct) → z-score → sort **ascending** (low→high; Q5 =
top quintile, receives the integer remainder — factorwatch convention).
Fixed 2026-08-29: the sort was previously descending, which inverted every
factor's long/short books (sign-inverted spreads since inception).
**Output**: `{quintiles: [Q1..Q5], longOnly: Q5, shortOnly: Q1, spread: Q5+Q1}`.

### 14.3 `computeSpreadMonitor(portfoliosByFactor, candlesBySymbol, benchmarkUniverse)`

For each factor: compute equal-weighted Q5, Q1, benchmark series; compute
relative (Q5 − benchmark) and spread (Q5 − Q1) returns at 1d/5d/20d/60d +
YTD; each with `{ret, z, pctile}` via `pairHorizonReturnWithStats`
(pair-return definition: `A[t]/A[t−h] − B[t]/B[t−h]`; replaced the old
`horizonReturnWithStats` zero-crossing-difference baseline which produced
±400% artifacts — 2026-08-29 audit).
**Output**: `{factor: {factor, label, rel_1d, spread_1d, rel_5d, spread_5d,
rel_20d, spread_20d, rel_60d, spread_60d, rel_ytd, spread_ytd}}`.
**Used by**: `compute_crypto_factors`, `FactorMonitor`, `compositeEngine`.
**In signal engine?**: YES — `spread_20d.z` is the primary input to
`computeFactorStance`.
**Predictive potential**: HIGH.

### 14.4 `detectFactorRotation(portfoliosByFactor, candlesBySymbol)` — snapshot leader

Snapshot-only: returns leader by 20d return + trailing 20d returns per
factor. Full rotation detection happens in `rotationDetector.detectRotation`
on the persisted history.
**Output**: `{leader_20d, leader_held_days: 0, flipped_from: null,
flip_flag: false, trailing_20d_returns: {...}}`.

### 14.5 `buildQuilt(portfoliosByFactor, candlesBySymbol)` — 13-month performance quilt

**Output**: `Array<{month: 'YYYY-MM', ranking: [{factor, label, return}]}>`.
**Used by**: `FactorQuilt` component.
**Predictive potential**: LOW (display).

### 14.6 Internal helpers

- `winsorize(arr, lower=0.025, upper=0.975)` — clip to percentile bounds
- `zScore(arr)` — mean/stddev normalisation
- `computeBeta(assetReturns, marketReturns)` — OLS β
- `buildEqualWeightSeries(symbols, candlesBySymbol)` — equal-weighted normalized price series
- `subtractSeries(a, b)` — REMOVED (2026-08-29 audit): normalised difference
  a_norm − b_norm fed spread z-scores from a zero-crossing series; replaced
  by `pairHorizonReturnWithStats` in `regimePercentile.js`
- `detectIdenticalQuintiles(portfoliosByFactor)` — diagnostic warning if two factors produce identical Q5
- `computeMonthlyReturns(priceSeries)` — approximate monthly returns (uses ~30 candles/month heuristic)

---

## 15. Scanner calculations

**File**: `src/lib/scanner/calculations.js` (89 lines)
**Used by**: `scanEngine.analyzeAsset`, `Scanner` page filters.

### 15.1 `calcEMA(closes, period)` — TradingView-compatible EMA

Seed: SMA of first `period` bars. Multiplier: `2/(period+1)`.
**Output**: number or null.

### 15.2 `calcVWAP(candles, days, candlesPerDay=6)` — rolling VWAP

Typical price = (H+L+C)/3. `VWAP = Σ(TP*V) / Σ(V)`.
**Output**: number or null.

### 15.3 `calcRSI(closes, period=14)` — Wilder's RSI

Seed: simple average of first `period` gains/losses. Wilder smoothing.
**Output**: 0–100, or null.

### 15.4 `fmtPrice(p)`, `fmtPct(p)` — display formatters

---

## 16. Scanner scan engine

**File**: `src/lib/scanner/scanEngine.js` (828 lines)
**Used by**: `src/pages/Scanner.jsx`.

### 16.1 `analyzeAsset(asset, settings, cgMarketData, hlTickers)` — trend filter pipeline

Two configurable trend gates (user can toggle each):
1. **priceAboveSlow**: `price > slow` (slow = EMA-200 or VWAP-30d)
2. **fastAboveMid**: `fast > mid` (fast = EMA-21 / VWAP-3d, mid = EMA-50 / VWAP-14d)

Plus optional:
3. **minVolume** filter (24h USD volume)
4. **minMarketCap** filter
5. **rsi** band filter (custom period + timeframe)

**Per-asset output**: `{...asset, price, emaFast, emaMid, emaSlow, pricePct,
emaPct, change24h, sparkline, volume24h, marketCap, marketCapRank,
fundingRate, openInterest, openInterestRaw, rVol, rsi}`.

### 16.2 `computeRVol(candles, period=20)` — relative volume

`currentVol / SMA(prior 20 vols)`. rVol > 1 = volume surge.
**Used by**: Scanner results, returned in asset row.
**In signal engine?**: No (filter-only).
**Predictive potential**: MEDIUM — volume confirmation of breakouts.

### 16.3 `runScan(settings, onProgress)` — orchestrator

Fetches top-300 universe, market data, Hyperliquid tickers; runs
`analyzeAsset` in pool of 5; retries failures via auto-resolver.

---

## 17. Server-side crypto factor pipeline

**File**: `scripts/compute_crypto_factors.js` (608 lines)
**Used by**: `build_snapshot.js` (GitHub Actions 4× daily).

Mirrors the client-side `factorEngine` + `rotationDetector` +
`crowdingMatrix` for server-side persistence. Fetches top-100 CoinGecko →
OKX/Bybit 365d candles → computes factors → builds spread monitor + rotation
+ crowding → accumulates 90-day spread history → persists to `snapshot.json`.

### 17.1 `computeCryptoFactors(prevSnapshot)` — main entry point

**Output**: `{factorData, factorHistory, spreadHistory}` where `factorData`
contains `leader, leader_held_days, flip_flag, flip_confirmed,
previous_leader, trailing_20d_returns, spread_monitor[5 factors × 5
horizons], crowding: {matrix, max_correlations}, stances: {factor:
{stance, confidence, crowdingScore}}`.

### 17.2 `computeStanceFromSpread(spread20d, rotation, crowdingScore)` — server-side stance

Lightweight mirror of `compositeEngine.computeFactorStance` without the
React-incompatible imports. Same stance vocabulary + thresholds.

### 17.3 `fetchTopCryptoByMcap(limit=100)` — CoinGecko top-100 fetcher

### 17.4 `fetchCryptoCandles(symbol, limit=365)` — OKX SWAP → OKX SPOT → Bybit fallback chain

Server-side CORS-free candle fetcher. Same `{ts, open, high, low, close,
vol}` shape as client-side.

---

## 18. Signal verdict engine — STRONG / WEAK / NEUTRAL (implemented)

**File**: `src/lib/signal/compute.js` (575 lines, v3.1 — backtested & tuned)
**Used by**:
- `src/pages/Signal.jsx` (renders pre-computed `signal_metrics` from `snapshot.json`)
- `scripts/compute_signal_metrics.js` (production snapshot builder — runs server-side in GitHub Actions)
- `scripts/signal/backtest.js` (historical replay / parameter tuning)
- `scripts/signal/orthogonal.test.js` + `scripts/signal/compute.test.js` (112-test suite, `npm test`)

**Architecture**: weighted 9-signal scoring system (NOT a pass/fail gate chain).
Each signal contributes a directional weight + optional penalty/boost to a
composite stance and 0–10 confidence. The final verdict is mapped from
`(stance, confidence)` via `DEFAULT_THRESHOLDS`.

### 15.1 `computeAssetStance({ zScore, zPctile, trendTenure, atrExt, rsVsBtc, fundingZ, rsi, obvSlope, impulseZ, returns, macroZ, mhAlignment, isBtc, ablations })`

The core verdict function. Returns `{ stance, confidence, drivers }` where
`stance ∈ {STRONG, NEUTRAL, WEAK}` and `confidence` is 0–10.

**Signal stack** (from file header):

| # | Signal | Function | Role |
|---|--------|----------|------|
| 1 | adaptiveZ (90/365 blend) | `adaptiveZ(series, 90, 365, 0.6)` | Price z-score, regime-aware (primary driver) |
| 2 | adaptiveZWithPctile | `adaptiveZWithPctile(series, 90, 365, 252)` | Non-parametric percentile (fat-tail detection) |
| 3 | trendTenure | `computeTrendTenure(closes)` | Consecutive days closing above 50-MA (persistence) |
| 4 | atrExt50ma | `computeAtrExt50ma(candles)` | Extension from 50-MA in ATR units (volatility-normalized) |
| 5 | RS vs BTC (7-day) | `computeRsVsBtc(candles, btcCandles, 7)` | Return ratio for majors (confirmation) |
| 6 | fundingZ | `fundingZScore(fundingHistory, asOfTs, 90)` | Funding rate z-score (crowding/reversal risk). Note: gate 6 is effectively dead in v3.1 per code comment — historical funding data was incomplete during backtest, so its weight is suppressed |
| 7 | RSI (penalty only) | `computeRSI(closes, 14)` | Overbought penalty (>80 reduces STRONG confidence) |
| 8 | impulseZ (penalty only) | `computeImpulseZ(closes, 13, 52)` | Decelerating penalty (falling momentum reduces STRONG) |
| 9 | macroZ (boost) | `computeMacroZ(candles, params)` | External macro signal, boosts confidence 7→8 when macroZ > 1.5 |

### 15.2 `mapStanceToVerdict(stance, confidence, thresholds)`

Maps the composite stance to a verdict label. `DEFAULT_THRESHOLDS` controls
the confidence cutoffs for STRONG vs NEUTRAL vs WEAK.

### 15.3 `computeSignal({ ... })`

Top-level orchestrator that wires all 9 signals together for a single asset.
Accepts pre-fetched inputs (candles, btcCandles, fundingHistory, macroState)
and returns the final verdict + driver breakdown used by the /signal page.

### 15.4 Backtest results

From the file header (2023-10 to 2025-07, 10-day forward, BTC/ETH/SOL):
- **STRONG**: 62.0% hit rate, +5.02% avg 10-day return (363 signals)
- **WEAK**:   54.1% hit rate, -1.37% avg 10-day return (61 signals)
- **NEUTRAL baseline**: +2.99% avg

### 15.5 Ablation support

`computeAssetStance` accepts an `ablations` parameter (Set or array of
strings) that neutralizes individual signals for backtest sensitivity
analysis. Disabling a signal zeros its contribution to the stance/confidence
calculation. Recognized ablation keys: `adaptiveZ`, `trendTenure`,
`atrExt50ma`, `rsVsBtc`, `fundingZ`, `macroZBoost`, `mhAlignment`,
`returns`, `rsiPenalty`, `impulseZPenalty`.

### 15.6 Related: `src/lib/signal/orthogonal.js` (OrthoSys v6.1)

A separate 9-signal orthogonal weighting system (Gram-Schmidt decorrelated)
that produces a composite z-score for intraday timing. Exported
`SIGNAL_NAMES` list: `vol_ratio`, `bb_width`, `rsi_signal`, `zscore_20`,
`mom_6`, `mom_18`, `ema_cross`, `hl_mom`, `taker_ratio`. See
`docs/ORTHOSYS_BACKTEST_REPORT.md` and `docs/ORTHOSYS_DAILY_BACKTEST_REVISED.md`
for backtest methodology and results.

---

## Summary statistics

- **Total signals catalogued**: 89 distinct calculations across 17 files.
- **Verdict engines** (produce STRONG/WEAK/NEUTRAL-style labels): 4
  - `boardEngine.themeStatus` (9-state theme verdict)
  - `regimeSignals.computeAllocation` (4-state allocation verdict)
  - `factors.compositeEngine.computeFactorStance` (4-stance factor verdict)
  - `signal/compute.computeAssetStance` (3-state STRONG/NEUTRAL/WEAK verdict — see §15)
- **Statistical primitives** reused across the codebase: 5
  - `adaptiveZ`, `horizonReturnWithStats`, `weightedComposite`,
    `computeRSI`, `computeOBV`
- **Files NOT mentioned in the original task but contain signals**: 5
  - `regime/regimeRotation.js`, `regime/regimeEngine.js`,
    `regime/seasonality.js`, `regime/changeLog.js`, `regime/factorSignals.js`
- **Pages that consume signals**: 4 (Scanner `/`, Board `/board`, MacroRegime `/macro`, Signal `/signal`)

---

*Document end. Last updated: 2026-08-11 — `src/lib/signal/compute.js` is now
implemented (v3.1, backtested); the "Recommended new signals" design proposal
in the original §15/§18 has been replaced with a real description of the
implemented engine. Earlier "does not exist" status sections were stale.*
